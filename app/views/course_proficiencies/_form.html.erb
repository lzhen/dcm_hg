<%= form_for(@course_proficiency) do |f| %>
  <% if @course_proficiency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_proficiency.errors.count, "error") %> prohibited this course_proficiency from being saved:</h2>

      <ul>
      <% @course_proficiency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
