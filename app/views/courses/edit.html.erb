<%= content_sheet_div %>

<div class="section">
  
	<%= error_messages_for :course %>
	
  	<%= form_for(:course, :url => course_path(@course), :html => { :method => :put }) do |form| %>
  
    	<%= render :partial => "edit_course", :locals => { :form => form } %>
    
    	<div class="menu FloatLeft FloatClear">
	      <%= submit_tag " UPDATE " %>
	    </div>
  
  <% end -%>
  
</div>

</div>
